# TravelAssistant API

FastAPI scaffold for `/api/v1`.

Run locally from the repository root:

```powershell
python -m uvicorn app.main:app --app-dir apps/api --reload
```

The scaffold only exposes health and root metadata. Provider-backed features are added in later steps.
